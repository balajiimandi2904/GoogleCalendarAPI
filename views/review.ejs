<!DOCTYPE html>
<html>
  <head>
    <title>Review Event</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="container py-5">
    <h1 class="mb-4 text-warning">Review Event</h1>

    <div class="card shadow-sm p-4 mb-4">
      <p><strong>Title:</strong> <%= event.summary %></p>
      <p><strong>Start:</strong> <%= event.start.dateTime %></p>
      <p><strong>End:</strong> <%= event.end.dateTime %></p>
      <% if(event.reminders.overrides.length > 0) { %>
      <p>
        <strong>Reminder:</strong> <%= event.reminders.overrides[0].minutes %>
        mins before
      </p>
      <% } else { %>
      <p><strong>Reminder:</strong> None</p>
      <% } %>
    </div>

    <form action="/addevent" method="POST" style="display: inline">
      <button type="submit" class="btn btn-success">Accept</button>
    </form>

    <form action="/declineevent" method="POST" style="display: inline">
      <button type="submit" class="btn btn-danger">Decline</button>
    </form>
  </body>
</html>
